name: Docker Image CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build the Docker image
      env:
        USER: ${{ secrets.USER }}
        PASS: ${{ secrets.PASS }}
      run:  |
        echo $USER
        echo $PASS
        sudo docker build . --file Dockerfile --tag tej-name/dcm_webserver_tej3027
        sudo docker login -u $USER -p $PASS
        sudo docker push tej-name/dcm_webserver_tej3027
